services:
  - type: web
    name: betting-bot
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node dist/main.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080
```

4. **Save the file**

---

### Step 2: Create `.gitignore`

**Location:** Root of your project

1. **New File** → Name it: `.gitignore` (yes, with the dot at the beginning)
2. **Open it** and paste this:
```
# Dependencies
node_modules/

# Build output
dist/

# Logs
logs/
*.log

# Environment variables
.env

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Data files (don't commit sensitive data)
config.json
users.json

# PM2
.pm2/
